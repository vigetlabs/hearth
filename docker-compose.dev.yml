services:
  db:
    image: postgres:16
    env_file:
      - ./services/.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build:
      context: ./api
      dockerfile: Dockerfile.dev
    env_file:
      - ./api/.env
    ports:
      - "8000:8000"
    depends_on:
      - db
    volumes:
      - ./api:/rails
      - bundle_data:/usr/local/bundle

volumes:
  postgres_data:
  bundle_data:
