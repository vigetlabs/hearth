// "Product" section of the sign-in page, sitting between the home section and
// "Why Hearth". Not designed yet — it renders nothing for now, so the page
// reads exactly as it does today and this is where that content will land.
export default function Product() {
  return null;
}
