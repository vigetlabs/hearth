// Viget article section of the sign-in page, the last section before the
// footer. Not designed yet — it renders nothing for now, so the page reads
// exactly as it does today and this is where that content will land.
export default function VigetArticle() {
  return null;
}
