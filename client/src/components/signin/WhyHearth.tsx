// "Why Hearth" section of the sign-in page, sitting between "Product" and the
// Viget article section. Not designed yet — it renders nothing for now, so the
// page reads exactly as it does today and this is where that content will land.
export default function WhyHearth() {
  return null;
}
